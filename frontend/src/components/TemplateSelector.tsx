// Premium Template Gallery Component - May 7, 2026
// Displays 5 JSON Resume templates with category filters

import { templates } from '../services/jsonResumeAdapter';

export const TemplateSelector = ({ currentTemplate, onSelect }) => {
  // Grid layout, category tabs, hover effects, gold gradient buttons
  return ( /* JSX for template cards */ );
};
